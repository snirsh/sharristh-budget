# =====================================================
# SharristhBudget Environment Configuration
# =====================================================
# Copy this file to .env.local and fill in your values
# =====================================================

# Database
# PostgreSQL (required for Vercel deployments):
DATABASE_URL="postgresql://user:password@host:5432/dbname?connection_limit=5&pool_timeout=20&connect_timeout=10"
#
# For local development, you can use:
# - PostgreSQL (via Docker, Neon, or local install)
# - Or temporarily set to SQLite: DATABASE_URL="file:./dev.db" (requires changing schema.prisma provider to "sqlite")
#
# Connection Pool Parameters (for PostgreSQL):
# - connection_limit: Max connections (recommended: 5 for 2-core server)
# - pool_timeout: Seconds to wait for connection (recommended: 20)
# - connect_timeout: Seconds to wait for initial connection (recommended: 10)

# Authentication (NextAuth.js + WebAuthn)
# Generate a random secret: openssl rand -base64 32
AUTH_SECRET=your-auth-secret-here-min-32-characters
# For local development, use 'localhost'
# For production, use your domain (e.g., 'budget.example.com')
AUTH_WEBAUTHN_RP_ID=localhost
AUTH_WEBAUTHN_RP_NAME=Sharristh Budget
# Must match your application URL
AUTH_WEBAUTHN_RP_ORIGIN=http://localhost:3000

# AI-Powered Category Suggestions (Optional)
# Set to 'true' to enable AI-powered transaction categorization
# Requires Ollama to be installed and running locally
# See AI_CATEGORIZATION.md for complete setup instructions
OLLAMA_ENABLED=false
# Ollama API base URL (default: http://localhost:11434)
OLLAMA_BASE_URL=http://localhost:11434
# Ollama model to use (default: llama3.2:3b - requires 3.5GB download)
# Alternatives:
#   - llama3.2:1b (1GB, faster but less accurate)
#   - llama3.1:8b (7GB, more accurate but slower)
OLLAMA_MODEL=llama3.2:3b

# Demo Mode (Optional)
# Set to 'true' to run in demo mode with sample data and no authentication
# Uses demo.db database and bypasses auth middleware
# See DEMO_MODE.md for details
DEMO_MODE=false

# =====================================================
# Quick Start
# =====================================================
# 1. Copy this file: cp .env.example .env.local
# 2. Generate AUTH_SECRET: openssl rand -base64 32
# 3. (Optional) Install Ollama for AI features
# 4. Run: pnpm dev
# =====================================================
