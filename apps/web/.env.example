# Auth.js Configuration
# Generate with: openssl rand -base64 32
AUTH_SECRET=your-secret-here

# WebAuthn Relying Party Configuration
# For localhost development, these defaults work fine
AUTH_WEBAUTHN_RP_ID=localhost
AUTH_WEBAUTHN_RP_NAME=Sharristh Budget
AUTH_WEBAUTHN_RP_ORIGIN=http://localhost:3000

# Admin Invite Code (one-time use for initial registration)
# Generate with: openssl rand -hex 16
# After registering, you can remove this or change it
ADMIN_INVITE_CODE=your-invite-code-here

# For production, update RP values:
# AUTH_WEBAUTHN_RP_ID=your-domain.com
# AUTH_WEBAUTHN_RP_ORIGIN=https://your-domain.com
